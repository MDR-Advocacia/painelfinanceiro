version: '3.8'

services:
  web:
    build: .
    # No desenvolvimento local, usamos o runserver para atualizar em tempo real
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"